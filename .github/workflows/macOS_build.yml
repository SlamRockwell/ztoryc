name: Ztoryc macOS Build

on:
  workflow_dispatch:
    inputs:
      publish_release:
        description: 'Create a versioned GitHub Release and upload DMG + PKG (uses release_tag)'
        type: boolean
        default: false
      release_tag:
        description: 'Tag name (e.g. v0.2.0); keep in sync with toonz/sources/include/tversion.h'
        type: string
        required: true
        default: 'v0.2.0'

jobs:
  macOS:
    runs-on: macos-15-intel
    permissions:
      contents: write
    env:
      HOMEBREW_NO_AUTO_UPDATE: '1'
      HOMEBREW_NO_ANALYTICS: '1'
      CCACHE_MAXSIZE: 5G
      CCACHE_COMPRESS: 'true'
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Setup cmake
        uses: jwlawson/actions-setup-cmake@v2
        with:
          cmake-version: '3.31.6'
      - name: Use cmake
        run: cmake --version
      - name: Restore Homebrew downloads cache
        id: brew-cache-restore
        uses: actions/cache/restore@v4
        with:
          path: ~/Library/Caches/Homebrew
          key: ${{ runner.os }}-brew-${{ hashFiles('ci-scripts/osx/tahoma-install.sh') }}
          restore-keys: |
            ${{ runner.os }}-brew-
      - name: Explain macOS CI hot vs cold paths
        run: |
          echo "Cold (deps cache miss): ffmpeg + opencv + libgphoto2 + TIFF (often ~1h+)."
          echo "Hot (deps cache hit): skips those heavy native builds; restores thirdparty TIFF build + /usr/local tree."
          echo "Every run still: brew installs (warm), cmake configure, full link of Ztoryc + packaging — not literally app-only;"
          echo "with ccache + unchanged sources that phase is commonly ~minutes–low tens of minutes, not hours."
      - name: Restore cache
        id: restore-cache
        uses: actions/cache/restore@v4
        with:
          path: |
            /Users/runner/.ccache
          key: ${{ runner.os }}-ccache-5G-${{ github.ref }}
          restore-keys: |
            ${{ runner.os }}-ccache-5G-
            ${{ runner.os }}-
      - name: Restore ffmpeg dependency cache
        id: restore-ffmpeg-cache
        uses: actions/cache/restore@v4
        with:
          path: |
            /usr/local/bin/ffmpeg
            /usr/local/bin/ffprobe
            /usr/local/lib/libav*.dylib
            /usr/local/lib/libsw*.dylib
            /usr/local/lib/libpostproc*.dylib
            /usr/local/lib/pkgconfig/libav*.pc
            /usr/local/lib/pkgconfig/libsw*.pc
            /usr/local/lib/pkgconfig/libpostproc.pc
            thirdparty/aom
            thirdparty/dav1d
            thirdparty/ffmpeg
          key: ${{ runner.os }}-ffmpeg-${{ hashFiles('ci-scripts/osx/tahoma-buildffmpeg.sh', 'ci-scripts/osx/tahoma-install.sh') }}

      - name: Restore OpenCV dependency cache
        id: restore-opencv-cache
        uses: actions/cache/restore@v4
        with:
          path: |
            /usr/local/lib/libopencv*.dylib
            /usr/local/include/opencv4
            /usr/local/lib/cmake/opencv4
            /usr/local/lib/pkgconfig/opencv4.pc
            thirdparty/opencv
          key: ${{ runner.os }}-opencv-v2-${{ hashFiles('ci-scripts/osx/tahoma-buildopencv.sh', 'ci-scripts/osx/tahoma-install.sh') }}

      - name: Restore libgphoto2 dependency cache
        id: restore-gphoto-cache
        uses: actions/cache/restore@v4
        with:
          path: |
            /usr/local/lib/libgphoto2*.dylib
            /usr/local/lib/libgphoto2
            /usr/local/lib/libgphoto2_port
            /usr/local/include/gphoto2
            /usr/local/lib/pkgconfig/libgphoto2*.pc
            thirdparty/libgphoto2_src
          key: ${{ runner.os }}-gphoto-${{ hashFiles('ci-scripts/osx/tahoma-buildlibgphoto2.sh', 'ci-scripts/osx/tahoma-install.sh') }}

      - name: Restore TIFF build cache
        id: restore-tiff-cache
        uses: actions/cache/restore@v4
        with:
          path: |
            thirdparty/tiff-4.2.0
          key: ${{ runner.os }}-tiff-${{ hashFiles('ci-scripts/osx/tahoma-build.sh', 'thirdparty/tiff-4.2.0/configure') }}
      - name: Install Dependencies
        run: |
          ci-scripts/osx/tahoma-install.sh
          brew install ccache
          mkdir -p /Users/runner/.ccache
      - name: Get CanonSDK
        if: ${{ (github.repository_owner == 'tahoma2d' || github.repository_owner == 'manongjohn') && github.event_name == 'push' }}
        run: |
          curl -H "Authorization: token ${{ secrets.TAHOMA2D_TOKEN }}" -H "Accept:application/octet-stream" -fsSL -o EDSDK_Macintosh.zip https://api.github.com/repos/tahoma2d/CanonSDK/releases/assets/223415237
          unzip EDSDK_Macintosh.zip -d EDSDK_Macintosh
          unzip EDSDK_Macintosh/Macintosh.dmg.zip -d EDSDK_Macintosh
          7z x EDSDK_Macintosh/Macintosh.dmg -oEDSDK_Macintosh
          mv EDSDK_Macintosh/Macintosh/EDSDK/* thirdparty/canon
          cd thirdparty/canon/framework/EDSDK.framework/Versions
          rm Current
          ln -s A Current
          cd ..
          rm Resources EDSDK
          ln -s Versions/Current/Resources Resources
          ln -s Versions/Current/EDSDK EDSDK
      - name: Restore thirdparty apps bundle cache (ffmpeg/rhubarb zips)
        id: restore-apps-cache
        uses: actions/cache/restore@v4
        with:
          path: thirdparty/apps
          key: ${{ runner.os }}-apps-${{ hashFiles('ci-scripts/osx/tahoma-get3rdpartyapps.sh') }}

      - name: Get 3rd Party Apps
        run: |
          ci-scripts/osx/tahoma-get3rdpartyapps.sh
      - name: Build ffmpeg (skipped when ffmpeg cache hit)
        if: ${{ steps.restore-ffmpeg-cache.outputs.cache-hit != 'true' }}
        run: |
          export PATH="/usr/local/opt/ccache/libexec:$PATH"
          ci-scripts/osx/tahoma-buildffmpeg.sh
      - name: Build OpenCV (skipped when OpenCV cache hit)
        if: ${{ steps.restore-opencv-cache.outputs.cache-hit != 'true' }}
        run: |
          export PATH="/usr/local/opt/ccache/libexec:$PATH"
          ci-scripts/osx/tahoma-buildopencv.sh
      - name: Build libgphoto2 (skipped when libgphoto2 cache hit)
        if: ${{ steps.restore-gphoto-cache.outputs.cache-hit != 'true' }}
        run: |
          export PATH="/usr/local/opt/ccache/libexec:$PATH"
          ci-scripts/osx/tahoma-buildlibgphoto2.sh
      - name: Build Ztoryc
        run: |
          export PATH="/usr/local/opt/ccache/libexec:$PATH"
          ci-scripts/osx/tahoma-build.sh
      - name: Create Package
        run: bash ./ci-scripts/osx/tahoma-buildpkg.sh
      - uses: actions/upload-artifact@v4
        with:
          name: Ztoryc-portable-osx.dmg
          path: toonz/build/Ztoryc-portable-osx.dmg
      - uses: actions/upload-artifact@v4
        with:
          name: Ztoryc-install-osx.pkg
          path: toonz/build/Ztoryc-install-osx.pkg
      - name: Create release tag
        if: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.publish_release == 'true' }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          TAG="${{ github.event.inputs.release_tag }}"
          git fetch origin --tags --force
          if git rev-parse "$TAG" >/dev/null 2>&1; then
            echo "Tag $TAG already exists at $(git rev-parse "$TAG")"
          else
            git tag -a "$TAG" -m "Release $TAG"
            git push origin "$TAG"
          fi
      - name: Publish GitHub Release (versioned)
        if: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.publish_release == 'true' }}
        uses: ncipollo/release-action@v1
        with:
          allowUpdates: true
          artifactErrorsFailBuild: true
          artifacts: toonz/build/Ztoryc-portable-osx.dmg,toonz/build/Ztoryc-install-osx.pkg
          artifactContentType: "raw"
          name: Ztoryc ${{ github.event.inputs.release_tag }}
          body: |
            Ztoryc macOS build — portable DMG and installer PKG.

            Built from commit ${{ github.sha }}.
          prerelease: false
          replacesArtifacts: true
          tag: ${{ github.event.inputs.release_tag }}
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Get Nightly Release Date
        if: ${{ github.repository_owner == 'tahoma2d' && github.event_name == 'push' && github.ref == 'refs/heads/master' }}
        run: |
          echo "NIGHTLYDATE=$(date '+%Y-%m-%d')" >> $GITHUB_ENV
          echo "NIGHTLYDATETIME=$(date '+%Y-%m-%d %H:%M:%S %Z')" >> $GITHUB_ENV
      - name: Create/Update Nightlies Tag
        if: ${{ github.repository_owner == 'tahoma2d' && github.event_name == 'push' && github.ref == 'refs/heads/master' }}
        run: |
          git remote add nightly_repo https://tahoma2d:${{ secrets.TAHOMA2D_TOKEN }}@github.com/tahoma2d/tahoma2d_nightlies.git
          git config lfs.https://tahoma2d:${{ secrets.TAHOMA2D_TOKEN }}@github.com/tahoma2d/tahoma2d_nightlies.git.lfs.locksverify false
          git fetch --unshallow
          git tag -f nightly
          git push -f origin nightly
          git tag -f nightly-${{ github.sha }}
          git config --unset-all http.https://github.com/.extraheader
          git push -f nightly_repo nightly-${{ github.sha }}
          git remote rm nightly_repo
      - name: Update Latest Nightly Release
        if: ${{ github.repository_owner == 'tahoma2d' && github.event_name == 'push' && github.ref == 'refs/heads/master' }}
        uses: ncipollo/release-action@v1
        with:
          allowUpdates: true
          artifactErrorsFailBuild: false
          artifacts: toonz/build/Ztoryc-portable-osx.dmg,toonz/build/Ztoryc-install-osx.pkg
          artifactContentType: "raw"
          body: ${{ github.event.head_commit.message }}
          name: Latest Nightly ${{ env.NIGHTLYDATE }}
          prerelease: true
          replacesArtifacts: true
          tag: nightly
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Create Nightlies Release
        if: ${{ github.repository_owner == 'tahoma2d' && github.event_name == 'push' && github.ref == 'refs/heads/master' }}
        uses: ncipollo/release-action@v1
        with:
          allowUpdates: true
          artifactErrorsFailBuild: false
          artifacts: toonz/build/Ztoryc-portable-osx.dmg,toonz/build/Ztoryc-install-osx.pkg
          artifactContentType: "raw"
          body: ${{ github.event.head_commit.message }}
          name: ${{ env.NIGHTLYDATETIME }}
          omitBodyDuringUpdate: true
          omitNameDuringUpdate: true
          owner: tahoma2d
          prerelease: true
          replacesArtifacts: true
          repo: tahoma2d_nightlies
          tag: nightly-${{ github.sha }}
          token: ${{ secrets.TAHOMA2D_TOKEN }}
      - name: Remove existing master cache
        if: ${{ steps.restore-cache.outputs.cache-hit == 'true' && github.ref == 'refs/heads/master' }}
        run: gh cache delete "${{ runner.os }}-ccache-5G-${{ github.ref }}"
        env:
          GH_TOKEN: ${{ github.token }}
      - name: Save cache
        if: ${{ steps.restore-cache.outputs.cache-hit != 'true' || github.ref == 'refs/heads/master' }}
        uses: actions/cache/save@v4
        with:
          path: |
            /Users/runner/.ccache
          key: ${{ runner.os }}-ccache-5G-${{ github.ref }}
      - name: Save ffmpeg dependency cache
        if: ${{ steps.restore-ffmpeg-cache.outputs.cache-hit != 'true' }}
        uses: actions/cache/save@v4
        with:
          path: |
            /usr/local/bin/ffmpeg
            /usr/local/bin/ffprobe
            /usr/local/lib/libav*.dylib
            /usr/local/lib/libsw*.dylib
            /usr/local/lib/libpostproc*.dylib
            /usr/local/lib/pkgconfig/libav*.pc
            /usr/local/lib/pkgconfig/libsw*.pc
            /usr/local/lib/pkgconfig/libpostproc.pc
            thirdparty/aom
            thirdparty/dav1d
            thirdparty/ffmpeg
          key: ${{ runner.os }}-ffmpeg-${{ hashFiles('ci-scripts/osx/tahoma-buildffmpeg.sh', 'ci-scripts/osx/tahoma-install.sh') }}

      - name: Save OpenCV dependency cache
        if: ${{ steps.restore-opencv-cache.outputs.cache-hit != 'true' }}
        uses: actions/cache/save@v4
        with:
          path: |
            /usr/local/lib/libopencv*.dylib
            /usr/local/include/opencv4
            /usr/local/lib/cmake/opencv4
            /usr/local/lib/pkgconfig/opencv4.pc
            thirdparty/opencv
          key: ${{ runner.os }}-opencv-v2-${{ hashFiles('ci-scripts/osx/tahoma-buildopencv.sh', 'ci-scripts/osx/tahoma-install.sh') }}

      - name: Save libgphoto2 dependency cache
        if: ${{ steps.restore-gphoto-cache.outputs.cache-hit != 'true' }}
        uses: actions/cache/save@v4
        with:
          path: |
            /usr/local/lib/libgphoto2*.dylib
            /usr/local/lib/libgphoto2
            /usr/local/lib/libgphoto2_port
            /usr/local/include/gphoto2
            /usr/local/lib/pkgconfig/libgphoto2*.pc
            thirdparty/libgphoto2_src
          key: ${{ runner.os }}-gphoto-${{ hashFiles('ci-scripts/osx/tahoma-buildlibgphoto2.sh', 'ci-scripts/osx/tahoma-install.sh') }}

      - name: Save TIFF build cache
        if: ${{ steps.restore-tiff-cache.outputs.cache-hit != 'true' }}
        uses: actions/cache/save@v4
        with:
          path: |
            thirdparty/tiff-4.2.0
          key: ${{ runner.os }}-tiff-${{ hashFiles('ci-scripts/osx/tahoma-build.sh', 'thirdparty/tiff-4.2.0/configure') }}

      - name: Save thirdparty apps bundle cache
        if: ${{ success() && steps.restore-apps-cache.outputs.cache-hit != 'true' }}
        uses: actions/cache/save@v4
        with:
          path: thirdparty/apps
          key: ${{ runner.os }}-apps-${{ hashFiles('ci-scripts/osx/tahoma-get3rdpartyapps.sh') }}

      - name: Save Homebrew downloads cache
        if: ${{ success() && steps.brew-cache-restore.outputs.cache-hit != 'true' }}
        uses: actions/cache/save@v4
        with:
          path: ~/Library/Caches/Homebrew
          key: ${{ runner.os }}-brew-${{ hashFiles('ci-scripts/osx/tahoma-install.sh') }}
